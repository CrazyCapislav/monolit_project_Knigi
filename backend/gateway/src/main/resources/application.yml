spring:
  application:
    name: gateway
  config:
    import: optional:configserver:${SPRING_CONFIG_IMPORT:http://localhost:8888}
# Конфигурация Gateway вынесена в Config Server (gateway.yml)
# Минимальная локальная конфигурация для запуска без Config Server